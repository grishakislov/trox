package view.tumor {
import flash.display.DisplayObject;

import view.controls.InteractiveView;

public class Tumor extends InteractiveView {

    public function Tumor(visual:DisplayObject) {
        super(visual);
    }
}
}
